package ggsimulator.models.neighborhoods;

import ggsimulator.models.Grain;

public class NeighborhoodWithRadius implements Neighborhood{
    @Override
    public int[][] getNeighbors(Grain grain) {
        return new int[0][];
    }
}
